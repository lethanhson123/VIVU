@using VIVU.Shared.Model
@model IEnumerable<TagModel>

@if(Model != null &&
   Model.Any())
{
    foreach(var tag in Model)
    {
        <div class="card w-100 p-0 mb-0 border-0">
            <div class="card-body p-1">
                <div class="row w-100 m-0">
                    <div class="col-md-10">
                        <h6 class="mb-0">@tag.Name - @tag.Title</h6>
                        <p class="mb-0" style="font-size:12px;">Từ khóa: @tag.Name</p>
                        <p class="mb-0" style="font-size:12px;">Mô tả: @tag.Description</p>
                    </div>
                    <div class="col-md-2">
                        <div class="float-end">
                            <button class="btn btn-outline-success mx-1" onclick="onDetail(@tag.Id)">
                                <i class="fa-solid fa-pen"></i>
                            </button>
                            <button class="btn btn-outline-danger mx-1" onclick="onDelete(@tag.Id)">
                                <i class="fa-solid fa-trash"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
}

